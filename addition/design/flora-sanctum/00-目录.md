# flora-sanctum 设计文档目录

## 项目一句话

flora-sanctum 是一个密码管理器：密码库是一个 Git 仓库，工作区中存放大量散碎的密文文件；加密用 Bouncy Castle，Git 操作用 JGit。核心性质是"明文的局部修改只引起密文的局部修改"，从而获得小粒度 diff、可合并的并发修改，以及"远端即备份"。

## 文档索引

| 编号 | 文档 | 主题 |
|---|---|---|
| 00 | 目录 | 本文件：目标、索引、修改指引 |
| 01 | 总体架构 | 模块划分、包结构、依赖方向 |
| 02 | 加密设计 | 密钥层次、KDF、AEAD、SECRET 对象格式 |
| 03 | 安全要点 | 内存、剪贴板、锁定、日志、威胁模型 |
| 04 | 存储设计 | 通用对象库：路径寻址、明文/密文分离 |
| 05 | 密码库适配器 | 密码管理 schema 如何插在通用存储之上 |
| 06 | Git 同步 | JGit、SSH 选型、同步模型、冲突策略 |
| 07 | UI 设计 | JavaFX 选型、桌面能力、UI-core 边界 |
| 08 | 依赖清单 | 坐标、版本、测试分工 |
| 09 | 实施路线 | 分阶段计划与验收标准 |

## 修改指引

每个文档只覆盖一个方面，架构审查与调整时：

- 改密码学方案（KDF/算法/文件格式）→ 只改 02
- 改安全策略/威胁模型 → 只改 03
- 改存储层（对象库、key、明文/密文分离）→ 只改 04
- 改上层 schema/条目模型/字段组织 → 只改 05
- 改同步策略/远端协议 → 只改 06
- 改界面框架/交互 → 只改 07
- 改依赖 → 只改 08
- 改计划/验收 → 只改 09

跨文档的联动修改（如换算法同时影响 02 与 08）需同时更新涉及文档，各文档头部"关联文档"用于提示此类联动。
