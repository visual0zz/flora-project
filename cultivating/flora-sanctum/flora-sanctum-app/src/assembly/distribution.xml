<?xml version="1.0" encoding="UTF-8"?>
<assembly xmlns="http://maven.apache.org/ASSEMBLY/2.1.0"
          xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
          xsi:schemaLocation="http://maven.apache.org/ASSEMBLY/2.1.0 https://maven.apache.org/xsd/assembly-2.1.0.xsd">
    <!-- flora-sanctum 分发 zip：lib/(应用 jar + 全量依赖) + 应用启动脚本（见设计"形态与启动"） -->
    <id>dist</id>
    <formats>
        <format>zip</format>
    </formats>
    <includeBaseDirectory>true</includeBaseDirectory>
    <baseDirectory>flora-sanctum</baseDirectory>
    <files>
        <!-- 应用形态启动脚本（start.cmd 非 edit 脚本，故分发版以应用形态启动，进入历史仓库列表页）。
             独立仓库由应用内"配置独立运行"生成 lib/ + edit/edit.bat，jar 据此判定孤立形态。
             明文偏好统一走系统级配置文件，仓库根不再放置 config.json。 -->
        <file>
            <source>src/main/resources/script/start.cmd</source>
            <outputDirectory>/</outputDirectory>
            <fileMode>0755</fileMode>
        </file>
    </files>
    <fileSets>
        <!-- 全量依赖 jar（maven-dependency-plugin 复制到 target/lib） -->
        <fileSet>
            <directory>${project.build.directory}/lib</directory>
            <outputDirectory>lib</outputDirectory>
            <includes>
                <include>*.jar</include>
            </includes>
        </fileSet>
        <!-- 应用自身 jar -->
        <fileSet>
            <directory>${project.build.directory}</directory>
            <outputDirectory>lib</outputDirectory>
            <includes>
                <include>${project.build.finalName}.jar</include>
            </includes>
        </fileSet>
    </fileSets>
</assembly>
