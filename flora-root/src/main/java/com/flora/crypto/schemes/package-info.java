/**
 * 具体加密方案（scheme）包。
 * <p>本包用于容纳具体的算法方案实现（如各类对称/非对称引擎、模式、MAC、签名器等），
 * 与 {@code com.flora.crypto.core} 中的抽象角色接口（Digest / BlockCipher / Signer …）对应。
 * 当前为占位空壳，后续实现将在此落地。</p>
 */
package com.flora.crypto.schemes;
