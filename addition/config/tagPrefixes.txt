# Git tag prefix list for push.cmd
# After pushing the branch, push.cmd also pushes every local tag
# whose prefix is listed here (one prefix per line).
# Lines starting with # are comments; blank lines are ignored.
ramet-idea-plugin-v
# ramet-vscode-v   # uncomment when VSCode plugin adopts version automation
